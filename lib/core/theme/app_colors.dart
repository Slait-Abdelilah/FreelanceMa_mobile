import 'package:flutter/material.dart';

class AppColors {
  // Brand — vert FreelanceMa
  static const brand500 = Color(0xFF14A800);
  static const brand600 = Color(0xFF0F8000);
  static const brand700 = Color(0xFF0A6000);
  static const brand100 = Color(0xFFE6F7E6);

  // Ink — texte sombre
  static const ink = Color(0xFF1A1A18);
  static const inkSoft = Color(0xFF73726C);
  static const inkMuted = Color(0xFF9C9A92);

  // Surfaces
  static const cream = Color(0xFFFAFAF7);
  static const surface = Color(0xFFFFFFFF);
  static const border = Color(0xFFEBEBE5);
  static const borderLight = Color(0xFFD3D1C7);

  // Sidebar hover/active
  static const sidebarActive = Color(0xFFF0EFE6);
  static const sidebarHover = Color(0xFFF4F4ED);

  // Status
  static const success = Color(0xFF14A800);
  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);
}
